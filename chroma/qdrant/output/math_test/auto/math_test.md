## 1 Basic Algebra

The quadratic formula is:

$$
x = { \frac { - b \pm { \sqrt { b ^ { 2 } - 4 a c } } } { 2 a } }\tag{1}
$$

## 2 Calculus

The definition of a derivative:

$$
f ^ { \prime } ( x ) = \operatorname* { l i m } _ { h \to 0 } { \frac { f ( x + h ) - f ( x ) } { h } }\tag{2}
$$

A definite integral:

$$
\int _ { 0 } ^ { \pi } \sin ( x ) d x = 2\tag{3}
$$

## 3 Linear Algebra

A matrix and its determinant:

$$
A = { \binom { a } { c } } \ b \quad \operatorname* { d e t } ( A ) = a d - b c\tag{4}
$$

## 4 Probability

The normal distribution probability density function:

$$
f ( x ) = { \frac { 1 } { \sigma { \sqrt { 2 \pi } } } } e ^ { - { \frac { 1 } { 2 } } \left( { \frac { x - \mu } { \sigma } } \right) ^ { 2 } }\tag{5}
$$

## 5 Series

Euler’s identity and an infinite series:

$$
e ^ { i \pi } + 1 = 0\tag{6}
$$

$$
\sum _ { n = 1 } ^ { \infty } { \frac { 1 } { n ^ { 2 } } } = { \frac { \pi ^ { 2 } } { 6 } }\tag{7}
$$

## 6 Inline Math

Here is some text with an inline formula, $E = m c ^ { 2 }$ , embedded directly in a sentence. Another inline example: the golden ratio $\textstyle \varphi = { \frac { 1 + { \sqrt { 5 } } } { 2 } }$ appears frequently in nature.